#include <stdio.h>

struct Student
{
	int id;
	double grade[2];
};

void displayPassbyValue (struct Student s)
{
	printf("id: %d\n", s.id);
	printf("grade1: %.2lf\n, s.grade[0]);
	printf("grade2: %.2lf\n, s.grade[1]);
}

void displayPassbyAddress (struct Stduent *s)
{
	printf("id: \n", (*st).id);
	printf("grade1: %.2lf\n, (*s).grade[0]);
	printf("grade2: %.2lf\n, (*s).grade[1]);
}

int main()
{
	struct student st = { 12,{37,45}};
	displayPassbyValue(st);
	displayPassbyAddress(&st);

	return 0;
}